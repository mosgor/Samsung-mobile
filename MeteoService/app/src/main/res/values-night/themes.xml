<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MeteoService" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:windowBackground">@drawable/night_theme</item>
        <item name="android:splashScreenTheme" tools:targetApi="s">@color/white</item>
        <item name="android:statusBarColor">@color/darkBlue</item>
        <item name="android:navigationBarColor" tools:targetApi="s">@color/lightBlue</item>
    </style>
</resources>